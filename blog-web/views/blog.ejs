<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('navbar') %>
    <% if(blogs) {%>
        <div class="container">
            <div class="row">
                <div class="col-lg-2"></div>
                <div class="col-lg-8">
                    <div class="card mt-4">
                        <img class="card-img-top img-fluid" src="/images/titleImgs/<%= blogs.titleImgs %>" alt="" />
                        <div class="card-body">
                            <h3 class="card-title"><%= blogs.title %></h3>
                            <h4><%= blogs.description %></h4>
                            <p class="card-text"><%= blogs.content %></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    <% } %>
</body>
</html>