<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Kumbh Sans', sans-serif;
            scroll-behavior: smooth;
        }

        .form-console{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px 0;
        }

        .form-control{
            display: flex;
            flex-direction: column;
        }

        label{
            padding: 5px 0; 
            font-size: 1rem;
            font-weight: 700;
        }

        input {
            width: 500px;
            height: 35px;
            outline: none;
        }

        input[type="text"] {
            font-size:20px;
        }

        textarea{
            height: 200px;
            font-size: 20px;
        }

        button{
            margin-top: 6px;
            width: 50px;
            max-width: 100%;
            border-radius: 3;
            cursor: pointer;
        }

        button:active{
            transform: scale(.98);
        }

        .update{
            width: 80px;
            margin-right: 15px;
        }

        .back{
            align-items: center;
            justify-content: center;
            display: flex;
            text-decoration: none;
            color: black;
        }


        @media screen and (max-width: 600px){
            input {
            width: 430px;
            height: 35px;
            outline: none;
        }

            input[type="text"] {
                font-size:17px;
            }

            textarea{
                height: 200px;
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <%- include('navbarafter') %>
    <form class="form-console" method="POST" action="/update" >
        <div class="form-side">
            <input type="hidden" value="<%= blog._id %>" name="update_id">
            
            <div class="form-control">
                <label>Title</label>
                <input type="text" class="form-control" name="title" value="<%= blog.title %>">
            </div>
    
            <div class="form-control">
                <label>Description</label>
                <input type="text" class="form-control" name="description" value="<%= blog.description %>">
            </div>
            
            <div class="form-control">
                <label>Content</label>
                <textarea name="content"><%= blog.content %></textarea>
            </div>
    
            <div>
                <button class="update" type="submit">Update</button>
                <button class="update"><a href="/manage" class="back">Back</a></button>
            </div>
        </div>
    
    </form>
</body>
</html>